"""
Compatibility adapters for hunttech-bot-common library.

Provides thin adapter layers that let the existing bot code call the common
library without changing the bot's public API or business logic.
"""
